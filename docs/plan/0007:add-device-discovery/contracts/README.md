# 接口契约（Contracts）

本目录用于存放 Plan #0007 的接口契约。为避免形状混杂，契约按 `Kind` 拆分成不同文件（不要把 HTTP / UI Component / File format 混在同一文件里）。

本计划涉及：

- `ui-components.md`：Add device（含 discovery）相关 UI 组件与领域数据形状
- Discovery provider 的实现契约（Tauri IPC）见 Plan #0008：`docs/plan/0008:tauri-desktop-client/contracts/rpc.md`
