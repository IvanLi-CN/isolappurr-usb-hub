# Brand Marketing Asset Themes History

This topic establishes a durable contract for bright and dark marketing assets.
It retains the existing bright GitHub preview while adding theme-paired public
exports and replacing the stale bright poster export geometry. The dark poster
is preserved as its approved native `gpt-image-2` full-image output so product
detail is never degraded by a local image-processing pass.
