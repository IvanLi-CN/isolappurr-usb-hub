# History

## Creation

This spec consolidates the long-lived product capability for USB communication, firmware update, Wi-Fi provisioning, Desktop native proxy, and Web App control into `docs/specs/**`.

Legacy references remain in `docs/plan/**` until a dedicated cleanup approves deletion or full migration.
